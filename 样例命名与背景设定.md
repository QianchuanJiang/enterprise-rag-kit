# 样例命名与背景设定（需求层 · 锁定版）

> 本文把三个核心样例（A/B/C）+ 一个可选加分项（D）的**名称、功能、背景叙事**一次性钉死。
> 这是需求层产物：只定"是什么 / 干什么 / 怎么讲"，不碰实现。实现等三份功能文档（A 已出，B/C 待定）收口后再讨论。

---

## 一、命名体系：一个框架，三个递进式样例

为体现 **T3 要求的"架构设计思维"**，不把三个样例做成孤立 demo，而是包装成**同一套框架下的三次递增交付**——这比三个散件更有说服力，也正好对应我们已有的 `rag-kit` 底座（共享解析/检索/问答，差异在配置与上层）。

| 层级 | 名称 | 仓库名 | 一句话定位 |
|---|---|---|---|
| 框架 | **「知擎」企业级 RAG 知识库交付框架** | `rag-forge` | 一套可复用的企业知识库底座，支撑三类交付场景 |
| 样例 A | **知擎·解析** DocParse | `rag-forge-docparse` | 把最难解析的企业文档做对，且回答可溯源 |
| 样例 B | **知擎·合规** Guard | `rag-forge-guard` | 满足强监管行业的分级权限与不可篡改审计 |
| 样例 C | **知擎·信创** Edge | `rag-forge-edge` | 断网可用、一键部署、跑在信创环境 |
| 样例 D（可选） | **知擎·调度** Router | `rag-forge-router` | 规则→小模型→大模型三级路由，成本砍半 |

> 命名逻辑：中文产品名负责"讲给人听"，英文 repo 名负责"查 Git"。三者共用 `rag-forge` 底座，面试时一句"这是同一框架的三次场景化交付"即展现架构抽象能力。

---

## 二、各样例功能清单（需求级，非实现）

### 样例 A · 知擎·解析 DocParse
**目标买家 / 面试叙事**：制造业信息化部（设备手册）、法务（合同/判例）、金融研究（年报/招股书）。证明"通用方案解析错的复杂文档，我能做对"。

1. **多格式接入**：PDF（文字版 + 扫描版）、Word、Markdown/网页；批量入库，单文件失败降级不中断整批。
2. **解析质量（立身之本）**：
   - 跨页表格还原为「属性-值对」，不被切散；
   - 无文字层文档自动走 OCR；
   - 页眉页脚 / 目录页码 / 水印噪声自动清洗；
   - 标题层级保留，支撑按结构切片。
3. **检索**：稠密向量 + BM25 混合；两段式重排（粗筛→重排→精排）；Query 改写（口语→检索友好）；可解释召回（返回来源与命中理由）。
4. **问答**：强制溯源（每条结论带来源文档 + 页码）；无据拒答（置信度低于阈值输出"知识库未找到依据"）。
5. **评测与证据**：解析成功率、表格还原准确率、Recall@k、Faithfulness、拒答准确率；对照实验表（纯向量 → +BM25 → +重排 → +改写 的召回率递进）。
6. **演示物料**：README 指标表、3 分钟录屏、一篇踩坑文章。

### 样例 B · 知擎·合规 Guard
**目标买家 / 面试叙事**：金融、医疗、政企合规岗。证明"95% 竞争者做不出的分级权限 + 审计，我能做出来"。

1. **分级权限模型**：public / internal / restricted 三级 + 三角色（访客 / 员工 / 管理员）。
2. **越权拦截**：基于文档密级标签的检索/问答级访问控制，越权拦截率 100%（测试用例覆盖）。
3. **三角色演示**：同一问题，不同角色看到不同密级答案。
4. **强制溯源 + 无据拒答**：复用 A 基线。
5. **操作审计**：不可篡改审计日志（谁 / 何时 / 问了什么 / 命中哪些片段 / 是否被拦截）。
6. **合成密级标注**：在公开法规内容上构造密级标签（public/internal/restricted + 三角色）——**这是 B 的必要组成，不是偷懒**。
7. **评测**：越权拦截率、三角色答案差异率、审计链完整性。

### 样例 C · 知擎·信创 Edge
**目标买家 / 面试叙事**：政企 / 国企 / 数据不出内网机构（单价最高 8–30 万）。证明"断网可用、一键起、还能跑在信创环境"。

1. **离线一体包**：`docker compose` 一键起（向量库 + 推理 + API）。
2. **断网可用**：无外网依赖，断网自测通过。
3. **本地推理**：Ollama / vLLM 本地服务，可换模型。
4. **信创适配（独有）**：昇腾 NPU + 麒麟 OS 实测部署与性能数据——这是你简历里最宽的护城河，竞争者几乎为零。
5. **复用数据**：复用 A/B 数据，只换部署形态，边际交付成本极低。
6. **评测**：启动成功率、断网问答延迟（P95）、信创环境吞吐 / 显存占用。

### 样例 D · 知擎·调度 Router（可选加分项）
**目标买家 / 面试叙事**：成本敏感客户。证明"能把我最贵的 72B 模型调用砍半"。
1. **三级路由**：规则 / 关键词 → 7B 小模型 → 72B 大模型。
2. **成本工程**：按置信度路由，简单问题不调大模型，token 成本砍半。
3. **路由准确率评测** + 成本对照（vs 全量大模型）。

---

## 三、背景设定（讲给面试官听的故事）

### 3.1 市场背景
企业内部知识长期"搜不到、答不准"：文档散在共享盘 / OA / 企微 / 邮件，全文搜索只能匹配关键词；通用大模型又不能直接用——它没见过企业私有数据、会一本正经地编、且数据不能出内网。RAG 是当前唯一成本可控的解法，但不是接个 ChatGPT 就完事。

### 3.2 技术演进主线（三个样例 = 三道坎）
> 我在为企业做 RAG 知识库交付的过程中发现，低代码方案（Dify 类）只能做 demo，真正卡住企业落地的有三道坎，而每一道坎都对应一个竞争者稀少的能力断层：

1. **第一坎 · 文档解析**：企业的真实文档是扫描件、跨页大表、带页眉页脚的噪声 PDF。通用方案一解析就乱。→ **样例 A 攻克**。
2. **第二坎 · 合规可信**：金融 / 医疗 / 政企要求"不同角色看不同密级、且操作可追溯"。95% 的玩家做不出。→ **样例 B 攻克**。
3. **第三坎 · 私有化与信创**：政企要求数据不出内网、还能跑在昇腾 + 麒麟上。集成商嫌单子小不接，个人里能做的人极少。→ **样例 C 攻克**。
4. **（增效）第四坎 · 成本**：全量调大模型太贵。→ **样例 D 增效**。

### 3.3 个人定位（T3 叙事）
三个样例不是三个孤立玩具，而是**同一套「知擎」框架下的三次场景化交付**：底座（解析/检索/问答/评测）一次写好，三个场景只换配置与上层能力。这恰好证明 T3 要的"解决复杂问题 + 架构设计思维"——我会做抽象、会沉淀、会把一个能力复用到不同战场。

> 合规边界：背景统一表述为"为企业做 RAG 交付咨询"，**不出现现东家名称、不出现电力/民航等具域名域资产**，规避 NDA/竞业风险。信创（昇腾+麒麟）作为个人技术能力呈现，不绑定雇主。

### 3.4 面试讲述脚本（可直接背，约 180 字）
> "我做了一个企业级 RAG 知识库框架『知擎』，用三个递进样例证明三件事：第一，我能把最难解析的企业文档——扫描件、跨页表——做对，召回率从纯向量的 58% 提到 82%；第二，我能满足强监管行业的分级权限和不可篡改审计，越权拦截率 100%；第三，我能做到断网可用、一键部署、还能跑在昇腾+麒麟信创环境。这三个样例共用一套底座，新场景边际交付能压到 20 小时内。我想应聘的也是这种需要把复杂能力工程化落地的岗位。"

---

## 四、与已有产物的衔接

| 已有产物 | 本文如何使用 |
|---|---|
| `样例A_功能清单与验收标准.md` | A 的功能以此为准，本文做收敛与命名统一 |
| `样例达标标准与数据方案.md` | 数据用公开/合成、指标必须真实可复现——A/B/C/D 全部遵守 |
| `rag-freelance-kit/rag-kit/*` | 已封存的脚手架即"知擎"底座雏形，三样例的共享代码来源 |
| `RAG接单作战手册.md` / `样例项目设计_3+1.md` / `交付物料模板.md` | 背景叙事可直接复用到平台主页与谈单话术 |

**当前锁定状态**：命名 ✓、功能 ✓、背景 ✓。下一步 = 补 B/C 的功能清单与验收标准（D 待前 2–3 单后做）。
