# 样例作品集设计：3 个核心 + 1 个加分项

> 设计原则
> 1. **全部使用公开数据**，可开源、可公开演示，不涉及任何现东家资产
> 2. **每个样例解决一个真实的坑**，不是玩具 Demo
> 3. **三个样例共用一套底座代码**，差别只在配置与数据
> 4. **每个样例必须有可量化指标**，写进 README
> 5. 顺序不可调换：A 是底座，不做 A 后面全部返工

总成本估算：约 70 小时（不含注册平台与投单），分散到 4–5 个周末可行。

---

## 样例 A：异构文档知识库 —— 年报 / 招股书问答

**这是底座，必须第一个做。**

### 定位

对标客户：制造业（设备手册、工艺规程）、法务（合同、判例）、金融（研报、招股书）
解决的坑：复杂版式解析、跨页表格还原、页眉页脚噪声
预计工时：25 小时

### 数据集

| 项目 | 说明 |
|---|---|
| 来源 | 巨潮资讯网（cninfo.com.cn）公开上市公司年报、招股说明书 |
| 规模 | 30–50 份 PDF，约 5,000–10,000 页 |
| 特点 | 大量跨页表格、图文混排、部分扫描件、页眉页脚噪声重 |
| 许可证 | 公开披露文件，仅用于技术验证，演示时标注来源 |

> 选这份数据的原因：它是**公开数据里版式复杂度最接近企业真实文档**的。企业内部的设备手册、工艺规程、合同模板，坑型和年报高度一致。

### 核心实现点

1. **解析层（最关键的 80%）**
   - 文字版 PDF：Docling 或 PyMuPDF，保留标题层级与表格结构
   - 扫描件：PaddleOCR 识别 + 版面还原
   - 表格处理：大表格拆成「属性–值对」单独存储，不做整表向量化
   - 噪声清洗：正则去掉页眉页脚、目录页码、水印

2. **切片层**
   - 父子 Chunk：小 chunk 128 token 用于精确检索，大 chunk 512–1024 token 用于生成上下文
   - 文档感知：按 Markdown 标题层级切分，不按字符硬切
   - 表格作为独立 object 节点，不走文本切片

3. **检索层**
   - 混合检索：BGE-M3 密集向量 + BM25 关键词，RRF 融合
   - 两段式重排：粗筛 Top-50 → bge-reranker-v2-m3 → Top-10
   - Query 改写：把口语化提问改写成检索友好格式

4. **生成层**
   - 强制引用溯源：每个结论必须标注来源文档 + 页码
   - 无依据即拒答：置信度低于阈值时输出「知识库中未找到依据」

### 验收指标（写进 README）

| 指标 | 目标 | 说明 |
|---|---|---|
| 解析成功率 | ≥ 95% | 成功提取正文的页面占比 |
| 表格还原准确率 | ≥ 85% | 抽样 50 个表格人工核对 |
| Recall@10 | ≥ 85% | 人工标注 50 条问答对 |
| Faithfulness | ≥ 0.85 | RAGAS 评测 |
| Answer Relevancy | ≥ 0.80 | RAGAS 评测 |
| 拒答准确率 | ≥ 90% | 30 条「知识库中不存在」的问题 |

**对照实验（这是最有说服力的一栏）**：

| 配置 | Recall@10 |
|---|---|
| 纯向量检索 | 58% |
| 向量 + BM25 融合 | 70% |
| + 重排 | 79% |
| + Query 改写 | 82% |

> 把这张表放進 README。**它直接证明你知道坑在哪、以及怎么解决——这才是甲方愿意付溢价的原因。**

---

## 样例 B：带权限隔离与审计的合规知识库 —— 法规 / 制度问答

**这是您的护城河。平台上 95% 的竞争者做不了这一块。**

### 定位

对标客户：金融机构、医疗机构、合规岗、有密级要求的政企
解决的坑：分级权限、强制溯源、无据拒答、全链路审计
预计工时：20 小时（复用样例 A 的解析与切片层）

### 为什么这是差异化

您在电力、民航做的是内网隔离、岗位分级、全链路推理审计——**这套东西在强监管行业是刚需，但众包平台上几乎没人能做**。普通开发者做出来的知识库是「所有人看到所有内容」，这在金融和医疗是上不了线的。

### 数据集

| 项目 | 说明 |
|---|---|
| 来源 | 国家法律法规数据库（flk.npc.gov.cn）、证监会 / 金融监管总局公开规范性文件 |
| 规模 | 200–500 份文件，约 5,000 条条款 |
| 分级设计 | 自行构造密级标签（公开 / 内部 / 受限），构造 3 类角色（普通员工 / 合规专员 / 管理员） |

### 核心实现点

1. **分级权限**
   - 密级标签写入 Qdrant payload
   - 检索时用 payload filter 硬过滤，**不是生成后过滤**（后者等于没做）
   - 越权访问在检索层拦截，并有独立测试用例

2. **强制溯源**
   - 每个答案必须附带来源条款编号 + 原文片段
   - 无法溯源的内容不输出

3. **分级拒答**
   - 无依据 → 拒答
   - 有依据但超权限 → 返回「您无权查看此内容」，**不透露内容是否存在**

4. **全链路审计**
   - 记录：谁、什么时候、问了什么、召回了哪些片段、返回了什么、耗时多少
   - 审计日志不可篡改（追加写 + 哈希链）
   - 提供审计查询界面

5. **风控熔断**
   - Prompt 注入检测
   - 敏感词过滤
   - 单用户频次限制

### 验收指标

| 指标 | 目标 |
|---|---|
| 越权访问拦截率 | **100%**（这是硬指标，必须满分） |
| Faithfulness | ≥ 0.85 |
| 溯源可点击率 | 100%（每个结论都能点回原文） |
| 拒答准确率 | ≥ 90% |
| 审计日志完整率 | 100% |
| Prompt 注入拦截率 | ≥ 95%（构造 20 条注入样本） |

**演示脚本（准备 5 分钟的话术）**：

> 同一个问题，用三个不同角色登录，看到的答案不一样，且低权限角色不会知道高密级内容是否存在。后台审计日志完整记录了三次访问。

---

## 样例 C：单机离线私有化一体包

**这是通往 8–30 万整包的门票。**

### 定位

对标客户：政企、国企、有数据不出内网要求的机构（最高单价品类）
解决的坑：断网可用、一键部署、显存与延迟可控
预计工时：15 小时（复用 A / B 的全部上层）

### 核心实现点

1. **一键部署**：一条 `docker compose up` 起全套（Qdrant + Ollama/vLLM + API + 前端）
2. **模型量化**：Qwen3-14B 或 DeepSeek 蒸馏版，INT4（GPTQ/AWQ）量化
3. **离线自检**：断网环境下一键验证全链路可用
4. **资源画像**：给出不同硬件配置下的性能数据

### 性能数据表（必须实测，不要抄）

| 硬件配置 | 模型 | 首 Token 延迟 | 吞吐 | 显存占用 |
|---|---|---|---|---|
| 单卡 RTX 4090 24G | Qwen3-14B INT4 | 实测 | 实测 | 实测 |
| 双卡 4090 | Qwen3-32B INT4 | 实测 | 实测 | 实测 |
| 昇腾 910B（如有条件） | Qwen3-14B | 实测 | 实测 | 实测 |

> **昇腾这一栏是您独有的。** 平台上几乎没有人能提供昇腾环境的实测数据。哪怕只有一组数据，也能直接把您和竞争者区分开。如果没有昇腾硬件，就在 README 里写明「支持昇腾 + 麒麟信创环境适配，已有国网冀北生产级落地经验」——这是履历背书，同样是壁垒。

### 验收指标

| 指标 | 目标 |
|---|---|
| 部署耗时 | ≤ 30 分钟（从裸机到可提问） |
| 断网可用性 | 100%（拔网线后全功能正常） |
| 首 Token 延迟 | ≤ 1.5 秒（14B INT4，单卡 4090） |
| 量化后精度损失 | ≤ 3%（对比 FP16 评测分数） |

---

## 加分项 D：分层调度降本（规则 → 7B → 72B）

**锦上添花，但它是您简历里最有辨识度的那一栏。**

### 定位

对标客户：所有对 token 成本敏感的客户（尤其是 SaaS 与高频问答场景）
解决的坑：全部问题都走大模型，成本失控
预计工时：10 小时（复用 A / B）

### 核心实现点

复刻您简历中的「规则引擎 + 轻模型 7B + 重模型 72B 三层分工」：

| 层级 | 处理内容 | 成本 |
|---|---|---|
| L0 规则引擎 | 格式化查询、精确匹配、模板化回答 | 几乎为零 |
| L1 轻模型（7B） | 意图识别、Query 改写、简单问答、结果汇总 | 低 |
| L2 重模型（72B） | 复杂推理、多跳检索、最终生成 | 高 |

路由策略：L0 命中直接返回；L0 未命中走 L1 判断复杂度；复杂度超阈值才升级 L2。

算力削峰：并发高峰时自动降级到 L1，保证可用性。

### 验收指标

| 指标 | 目标 |
|---|---|
| L2 调用占比 | ≤ 30% |
| 综合成本下降 | ≥ 50%（对比全量走 L2） |
| 准确率损失 | ≤ 2% |
| P95 延迟 | ≤ 3 秒 |

**成本看板**：做一个实时展示「今日调用分布 / 成本 / 节省金额」的页面。甲方看到这个页面的第一反应通常是「这个人是真的做过生产的」。

---

## 三个样例的能力矩阵

| 能力 | A | B | C | D |
|---|---|---|---|---|
| 复杂版式解析 | ★★★ | ★ | ★ | |
| 混合检索 + 重排 | ★★★ | ★★ | ★ | ★ |
| 分级权限与审计 | | ★★★ | ★★ | |
| 幻觉防控与溯源 | ★★ | ★★★ | ★ | ★ |
| 离线私有化部署 | | ★★ | ★★★ | ★ |
| 信创适配（昇腾 / 麒麟） | | ★ | ★★★ | |
| 成本工程 | | | ★ | ★★★ |
| 评测与回归体系 | ★★★ | ★★ | ★ | ★★ |

**面试与谈单时的叙事逻辑**：

> 我有三套可演示的系统。一套证明我能把最难的文档解析对，一套证明我能满足合规与审计要求，一套证明我能在客户内网的信创环境里跑起来。它们共用一套底座，所以新场景的边际交付时间可以压到 20 小时以内。

---

## 开源与展示建议

1. **代码仓库**：GitHub 或 Gitee 双份，README 用中文，指标表放在最前面
2. **演示视频**：每个样例录 3 分钟视频，展示真实交互（不要剪辑痕迹太重）
3. **技术文章**：每做一个样例写一篇踩坑记录，发到掘金 / 知乎 / 公众号
   - 文章比代码更能带来订单。甲方看不懂代码，但看得懂「跨页表格怎么处理」
4. **平台主页**：把三个 README 的指标表整合成一段「交付能力说明」
5. **合规提醒**：不要放任何现东家的文档、代码、架构图；年报与法规数据标注来源

---

## 时间规划建议

| 阶段 | 内容 | 工时 | 建议节奏 |
|---|---|---|---|
| 阶段 1 | 底座 + 样例 A | 25h | 第 1–2 个周末 |
| 阶段 2 | 样例 B | 20h | 第 3–4 个周末 |
| 阶段 3 | 样例 C | 15h | 第 5 个周末 |
| 阶段 4 | 加分项 D | 10h | 有余力再做 |
| 阶段 5 | 物料 + 主页 + 试投 | 8h | 与阶段 3 并行 |

> **重要提醒**：您当前处于求职期（简历标注「在职·月内到岗」）。样例项目的复利价值在于「既是接单的作品集，也是面试的作品集」——一套投入，两处收益。但如果时间紧张，**优先保证求职**，样例做到样例 A + B 即可形成有效背书，C 和 D 可以拿到 offer 之后再补。
