# 样例技术栈与算力说明（需求层 · 选型版）

> 本文把「知擎」框架涉及的技术点逐一讲清：它是什么、有哪些可选、我们**推荐哪个**、以及**吃不吃本地算力**。
> 选型原则：中文企业场景优先、MIT/Apache 可商用、能离线、社区主流。所有选型均不绑定任何雇主资产。

---

## 0. 技术栈总览（五层架构）

```
┌─────────────────────────────────────────────────────────────┐
│  第 5 层 · 生成 (LLM)         GLM-4.6V-Flash 多模态 / Qwen3:14B 本地   │
├─────────────────────────────────────────────────────────────┤
│  第 4 层 · 重排 (Reranker)    bge-reranker-v2-m3              │
├─────────────────────────────────────────────────────────────┤
│  第 3 层 · 向量库 (Vector DB) Qdrant（compose / 单机）          │
├─────────────────────────────────────────────────────────────┤
│  第 2 层 · 嵌入 (Embedding)  BGE-M3（dense + sparse 一体）     │
├─────────────────────────────────────────────────────────────┤
│  第 1 层 · 解析 (Parsing/OCR) docling + MinerU + PaddleOCR    │
└─────────────────────────────────────────────────────────────┘
   编排：轻量自研 + Pydantic 配置   服务：FastAPI + Streamlit   部署：Docker Compose
```

---

## 1. 文档解析层（Parsing / OCR）

**是什么**：把 PDF / Word / 网页变成"带结构的干净文本 + 表格"。这是 RAG 的地基——解析错了，后面全错。

**可选方案**
| 工具 | 特点 | 适用 |
|---|---|---|
| **docling**（IBM） | 版式/表格还原强，输出结构化 Markdown | 通用文档（推荐主选） |
| **MinerU**（上海AI实验室） | 中文公式、复杂表、扫描件还原极好 | 中文年报/招股书/论文（备援） |
| MarkItDown（微软） | Office/HTML 转 MD，轻 | 非 PDF 文档 |
| unstructured | 老牌通用解析 | 兜底 |
| PaddleOCR | 中文 OCR 最强之一 | 扫描件（无文字层） |

**推荐**：解析主用 **docling**，中文复杂表/公式用 **MinerU** 备援；扫描件走 **PaddleOCR**。
**吃算力吗**：OCR 阶段轻微吃 GPU，CPU 也能跑（慢）。开发期用公开年报（文字版为主）几乎不吃算力。

---

## 2. 嵌入模型（Embedding）

**是什么**：把文本转成向量，是语义检索的"地基"。嵌入质量决定检索上限，后面 reranker/LLM 补不回来。

**可选方案（2026 现状）**
| 模型 | 参数量 | 维度 | 许可 | 特点 |
|---|---|---|---|---|
| **BGE-M3** | 568M | 1024 | MIT | 中英强，**dense+sparse+多向量一次完成**，轻量可商用（推荐） |
| Qwen3-Embedding-8B | 8B | 4096 | Apache 2.0 | MTEB 综合第一（~70.6），但重 |
| Qwen3-Embedding-0.6B | 0.6B | 1024 | Apache 2.0 | 轻量高精度，Ollama 可拉 |
| m3e / bge-large-zh | — | 768/1024 | — | 老牌中文，稳但已非最优选 |

**推荐**：**BGE-M3**。理由——① 一个模型同时输出稠密向量 + 稀疏向量，**混合检索无需另搭 BM25**；② MIT 许可可商用；③ 568M 轻量，CPU 可跑；④ 中文强。若机器够且追求极致精度，可换 Qwen3-Embedding-0.6B/8B。
**吃算力吗**：568M 模型，CPU 可跑（批处理慢），GPU 更快。开发期可走 API（硅基流动 / BAAI 官方），**零本地算力**。

---

## 3. 向量数据库（Vector DB）

**是什么**：存向量 + 做相似检索。B 样例的"权限过滤"也依赖它的 payload 过滤能力。

**可选方案**
| 数据库 | 特点 | 适用 |
|---|---|---|
| **Qdrant** | Rust 写，高性能，支持 payload 过滤 + 混合检索，单机 Docker 即可 | **生产 / 我们的推荐** |
| Milvus | 功能最全、分布式 | 百万级大规模 |
| Chroma | 本地嵌入、零配置 | 原型 / 小规模 |
| FAISS | Meta，内存检索极快，无持久化 | 开发期 / 学习 |
| pgvector | PostgreSQL 扩展 | 已有 PG 基础设施 |

**推荐**：**Qdrant 单机 Docker**。理由——断网可用（C 必需）、payload 过滤支撑 B 的权限隔离、轻量。开发期 A/B 可用内存 FAISS 或 Qdrant 均可。
**吃算力吗**：几乎不吃，CPU 即可。

---

## 4. 重排模型（Reranker）

**是什么**：先用嵌入粗筛 50–100 条，再用 cross-encoder 精排，取 Top-5~10 给 LLM。**RAG 里"性价比最高的精度提升"**。

**可选方案**
| 模型 | 参数量 | 许可 | 特点 |
|---|---|---|---|
| **bge-reranker-v2-m3** | 0.6B | Apache 2.0 | 多语言、快、各框架支持，**默认正确选项** |
| Qwen3-Reranker | 0.6B/4B/8B | Apache 2.0 | 精度天花板，32K 上下文，长文档更优 |

**推荐**：**bge-reranker-v2-m3**（"无聊但正确"的默认项）。
**吃算力吗**：比 embedding 重，GPU 友好；小数据 CPU 也能跑；亦可走 API。

---

## 5. 生成大模型（LLM）

**是什么**：基于检索到的片段生成答案，并强制附带来源（溯源）。

**可选方案**
| 模型 | 适用 | 许可/获取 |
|---|---|---|
| **Qwen3**（7B/14B/32B/72B） | 中文强、开源、Apache | Ollama/vLLM 本地 或 阿里云百炼 API |
| DeepSeek-V3/R1 | 推理强 | API / 本地 |
| Llama 3.3 70B | 开源通用标杆 | Ollama/vLLM |
| GLM / 智谱 | 中文 | API |

**推荐**：
- **开发期 / 在线演示（A/B）**：**智谱 GLM-4.6V-Flash 多模态免费档**（见第 11 节），OpenAI 兼容、128K 上下文、支持图文/PDF 输入与工具调用，**零成本起步**，且原生多模态可直接用于"扫描件/复杂版式"文档理解（见 11.5）。
- **在线演示备选**：**Qwen API**（硅基流动 / 阿里云百炼），中文效果更稳；纯文本生成亦可用 GLM-4.7-Flash（200K 上下文）降本。
- **离线（C）**：**Ollama / vLLM 起 Qwen3:14B** 本地；追求效果可上 32B/72B（需多卡或量化）。
**吃算力吗**：
- 7B：CPU 可跑（慢）/ 单卡 8G；
- 14B：约 12–16G 显存（一张 3090/4090 足够）；
- 72B：需多卡或 4-bit 量化。

---

## 6. 编排框架（Orchestration）

**可选**：LangChain（生态最全但有抽象损耗）/ LlamaIndex（专注 RAG）/ Haystack（企业级）/ **自研**。
**推荐**：**轻量自研 + Pydantic 配置驱动**。理由——面试要展现的是你的架构抽象能力，而非"会调 LangChain"；自研底座也让 A/B/C 共享代码，边际交付可压到 20 小时内。LlamaIndex 可作兜底。
**吃算力吗**：无。

---

## 7. 服务与演示（Serving）

- **FastAPI**：对外 API（ingest / ask / audit 接口）。
- **Streamlit**：3 分钟演示 UI（上传文档 → 提问 → 看溯源）。
- **Docker Compose**：C 样例一键编排（向量库 + 推理 + API），断网可用。

---

## 8. 算力总账：到底要不要本地 GPU？

**这是你最该看清的一点，分层回答：**

| 阶段 | 要不要本地算力 | 说明 |
|---|---|---|
| **开发期 / A、B 演示** | **可零本地算力** | 嵌入、重排、LLM 全走 API（硅基流动/百炼）；向量库用内存或云端。本机只需 Python + Docker |
| **跑一次真实指标** | 不需要 own GPU | 同一套 API 跑一遍、记录数字即可 |
| **样例 C 离线/信创** | **必须本地算力**（或客户环境提供） | 演示用一台 3090/4090（24G）跑 14B 足够；信创场景用客户昇腾环境 |
| **最低可行配置** | 16G 内存 + 普通 CPU 笔记本 | 即可开发 + 接 API 跑通；想本地跑 14B 再 + 一张 24G 显卡 |

**结论**：A、B 样例你用 API 就能做出"真实指标 + 可演示"，**不花钱买显卡**；只有 C（单价最高的那档）才需要真机器——而这恰恰是它贵、且竞争者少的原因。

---

## 9. 三个样例的技术栈组合表

| 组件 | A 解析 | B 合规 | C 信创 |
|---|---|---|---|
| 解析 | docling + MinerU | docling | 复用 A |
| 嵌入 | BGE-M3（可 API） | BGE-M3（可 API） | BGE-M3（本地） |
| 向量库 | Qdrant / FAISS | Qdrant（payload 过滤） | Qdrant（compose） |
| 重排 | bge-reranker-v2-m3 | 同上 | 同上（本地） |
| LLM | GLM-4.6V-Flash（免费·多模态） | GLM-4.6V-Flash（免费·多模态） | Qwen3:14B 本地 / Ollama |
| 是否需要本地 GPU | 否（可全 API） | 否（可全 API） | **是**（或客户昇腾） |

---

## 10. 与"公开/合成数据 + 真实指标"原则的衔接

- 模型用开源（BGE 系列 MIT/Apache）或 API 均可，**不影响"数据真实 vs 指标真实"原则**——评估脚本跑在你提交的公开/合成数据上，数字必须真实可复现。
- BGE-M3、bge-reranker-v2-m3、Qwen 系列均为 **MIT / Apache 2.0 可商用许可**，无授权风险，可放心写进作品与谈单。

> 一句话：A/B 用 API 零成本做出可信作品；C 才需要真机器，而那正是它的高单价护城河。

---

## 11. 开发期选型决策（已确认 · 2026-09-01）

### 11.1 生成模型：智谱 GLM-4.6V-Flash 多模态免费档（已采用 · 开发/演示阶段）

- **是否可用（已实跑验证）**：可用且已通过真实 API 调用验证。GLM-4.6V-Flash 由智谱（Zhipu AI）提供，**输入/输出均 $0**，128K 上下文，原生支持**文本 / 图像 / PDF / 视频**输入与工具调用，2026-05 起免费。已用 `scripts/test_llm_connectivity.py` 实测调通（`model=glm-4.6v-flash`，base_url 不变）。
- **接入方式**：**OpenAI 兼容**，仅需改 `base_url` 与 `api_key`，与现有 `rag-kit` 的 OpenAI 客户端无缝对接。
  - 网关：`https://open.bigmodel.cn/api/paas/v4`（注册即送免费档，无需实名即可用 Flash）
  - 模型名：`glm-4.6v-flash`（多模态；纯文本生成亦可用 `glm-4.7-flash`，200K 上下文）
- **限制（必须知道）**：
  - 免费档**并发 1 请求 / ≈1 次每秒**。开发、顺序批处理足够；**不能**支撑多用户并发公网服务。
  - 属"Flash"轻量档，推理/复杂多步规划弱于旗舰（GLM-5.x）。我们样例以 RAG 生成+溯源为主，够用。
- **数据出境提示**：请求会发往智谱云端。**A/B 开发演示无碍**；但到 B（合规权限）/ C（内网信创）阶段，必须切到**本地推理（Ollama/vLLM）**，把"数据不出内网"作为交付能力演示。即：免费云端只用于开发期，真实合规场景用本地底座。
- **密钥管理**：API key 存于本地 `.env`（`ZHIPU_API_KEY=...`），已被 `.gitignore` 屏蔽，绝不进仓库、绝不硬编码。

### 11.2 嵌入模型：本地 BGE-M3 为主 + 云端免费兜底

- **首选（推荐）· 本地 BGE-M3**：`ollama pull bge-m3`，**零成本、零额度、数据不出本机**，且天然契合 B/C 的"数据不出内网"叙事。CPU 可跑（批处理慢），GPU 加速更好。维度 1024、上下文 8192、100+ 语言、dense+sparse 一体。
- **云端免费兜底（无需装 Ollama）**：
  - **智谱 embedding-3**：新用户送 2000 万 tokens 免费档，中文优化，2048 维（默认 1024），8K 上下文，OpenAI 兼容（`https://open.bigmodel.cn/api/paas/v4`）。
  - **硅基流动 bge-m3**：完全免费、中国大陆直连、中英效果好，OpenAI 兼容（`https://api.siliconflow.cn/v1`）。
- **结论**：开发期默认走**本地 BGE-M3**；若本机不想跑 Ollama，则用**智谱 embedding-3 或硅基流动 bge-m3** 免费云端。两者均 OpenAI 兼容，切换仅改配置。

### 11.3 重排模型

开发期即可走 **bge-reranker-v2-m3** 本地（Ollama / sentence-transformers），或硅基流动 API。无需额外决策。

### 11.4 开发顺序（关键决策）

**不是"先采数据"或"先写底座"二选一，而是小闭环互验**：

1. **先锁抽象（约 1–2h）**：在 `rag-kit` 配置中确认 LLM 用智谱免费档、Embedding 用本地 BGE-M3，跑通"换供应商只改配置"。
2. **再取极小验证集（约 2–3 份年报）**：从巨潮网拉 2–3 份公开年报，**仅用于端到端冒烟测试**，不追求规模。
3. **打通底座闭环**：解析→切片→嵌入→检索→生成→评测，先用 stub 跑通结构，再接真实智谱+BGE-M3 出第一批真实指标。
4. **最后扩量迭代**：验证集跑绿后，再扩到 5–10 份，打磨 A 的完整功能与指标。

> 原因：底座代码此前已起草，但未用真实数据验证过；先拿极小样本验证，比"先大规模采数据"更省时、更早暴露问题。

### 11.5 多模态价值：Vision Parsing（样例 A 的差异化增强 · 设计待定）

GLM-4.6V-Flash 是**原生多模态**模型，原生接收图像 / PDF 输入。这给样例 A（异构文档解析）提供一条比"OCR+版式解析"更直接的路径：

- **传统路径**：PDF → docling/MinerU 解析 → 表格还原 → 切片（对扫描件、跨页表、水印污染易出错）。
- **多模态路径**：扫描页 / 复杂版式页 → **直接喂图给 GLM-4.6V-Flash** → 输出结构化 Markdown / 表格 JSON（模型直接"看懂"版式）。
- **定位**：多模态解析不是替代 docling，而是**互补**——docling 处理文字版 PDF 的版式还原，VLM 处理扫描件 / 图像型复杂表，二者结果统一进切片层。这正好坐实我们"把最难解析的文档做对"的卖点。
- **设计状态**：本期（开发期）先以文本问答跑通 LLM 配置；是否把 Vision Parsing 作为样例 A 的独立功能模块，待与用户确认后再进入实现。

---

## 12. 与"公开/合成数据 + 真实指标"原则的衔接（补充）

- 开发期用**智谱免费档 + 本地 BGE-M3**，零成本即可产出真实可复现指标，完全契合"数据公开/合成 + 指标真实"原则。
- BGE-M3、bge-reranker-v2-m3、Qwen / GLM 系列均 MIT / Apache 2.0 / 可商用，无授权风险，可放心写进作品与谈单。
