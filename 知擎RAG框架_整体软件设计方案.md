# 「知擎」企业级 RAG 知识库框架 · 整体软件设计方案

> 版本：v1.0 ｜ 日期：2026-09-01 ｜ 状态：需求/设计层收口版（未进入实现）
> 性质：把分散的 5 份需求文档收口为单一总纲，作为后续实现与面试/接单的统一基线。

---

## 0. 阅读指引

本文是**需求与设计层的总纲**，回答"做什么、为什么、长什么样、怎么交付"，不含具体代码。三类读者视角：

| 读者 | 关注点 | 重点章节 |
|---|---|---|
| 面试官（T3 认证） | 架构思维、工程判断力 | 第 2、5、7、8 章 |
| 自己（实现路线） | 模块边界、里程碑、技术选型 | 第 5、6、8、11 章 |
| 接单平台（能力背书） | 能交付什么、差异化在哪 | 第 3、7、12 章 |

详细的分样例验收标准见附录文档清单；本文对 A 的验收给出完整版，B/C/D 给出设计层指标，细化文档后续补。

---

## 1. 背景与问题定义

企业内部的专有知识长期"搜不到、答不准"：文档散落在共享盘、OA、企微、邮件，全文搜索只能做关键词匹配；而通用大模型又不能直接用于企业私有数据——它没见过企业内部资料、会一本正经地编造、且数据不允许出内网。**RAG（检索增强生成）是当前唯一成本可控的解法**，但"接个 ChatGPT 就是知识库"是普遍误解，真实落地有三道坎，且每道坎都对应一个竞争者稀少的能力断层：

1. **文档解析**：真实企业文档是扫描件、跨页大表、带页眉页脚的噪声 PDF，通用方案一解析就乱。
2. **合规可信**：金融/医疗/政企要求"不同角色看不同密级、操作可追溯"，95% 的玩家做不出。
3. **私有化与信创**：政企要求数据不出内网、还能跑在昇腾+麒麟上；集成商嫌单子小不接，能做的人极少。

本框架即围绕这三道坎构建可复用的交付能力。

---

## 2. 核心概念边界：知识库 vs 智能体

这是定位的底层问题，必须先厘清。**知识库（RAG）本身不是智能体**，二者是不同抽象层级：

- **知识库 / RAG**：被动问答系统——提问 → 检索私有文档 → 生成带引用的答案。无自主行动能力。
- **智能体 / Agent**：带"感知—规划—记忆—行动"闭环的系统，LLM 当大脑做决策，通过工具（tool）调用数据库/搜索/API/代码来**自主推进目标直至把事办完**。

二者关系有三种（也是本框架的演进路径）：

| 关系 | 说明 | 对应形态 |
|---|---|---|
| RAG 是 Agent 的组件 | Agent 把知识库当"查资料"的工具调用 | 企业智能体 = RAG + 工具集 |
| 加"规划+工具+行动"编排 | 在知识库上叠加自主编排层即升级为 Agent | 从蓝框到紫框 |
| 中间态 Agentic RAG | 检索本身变聪明：自主决定何时检索、多跳检索、反思后补检索 | 介于二者之间 |

**本框架与三个样例的定位：处于"引擎层 / 地基"**——即 RAG 这一层，是任何企业智能体都绕不开、且最难、最值钱的那块。这一定位与简历中的"智能体产品负责人"完全自洽：对外表述为"**我做的是企业智能体的知识中枢 / RAG 引擎，并具备把它升级为端到端智能体的能力**"。是否单独做一个"真·智能体"样例？**先把三个地基做扎实**（覆盖 80% 说服力与 80% 订单入口），agent 编排层作为后续加分项。

---

## 3. 产品定位与命名体系

为体现 T3 要求的"架构设计思维"，三个样例不做成孤立 demo，而是**同一套框架下的三次递增交付**——底座（解析/检索/问答/评测）一次写好，三个场景只换配置与上层能力。

| 层级 | 名称 | 仓库名 | 一句话定位 |
|---|---|---|---|
| 框架 | **「知擎」企业级 RAG 知识库交付框架** | `rag-forge` | 可复用的企业知识库底座，支撑三类交付场景 |
| 样例 A | **知擎·解析** DocParse | `rag-forge-docparse` | 把最难解析的企业文档做对，回答可溯源 |
| 样例 B | **知擎·合规** Guard | `rag-forge-guard` | 分级权限 + 不可篡改审计（竞争者最少） |
| 样例 C | **知擎·信创** Edge | `rag-forge-edge` | 断网可用、一键部署、跑在信创环境（单价最高） |
| 样例 D（可选） | **知擎·调度** Router | `rag-forge-router` | 规则→小模型→大模型三级路由，成本砍半 |

中文产品名负责"讲给人听"，英文 repo 名负责"查 Git"。面试时一句"这是同一框架的三次场景化交付"即展现架构抽象能力。

---

## 4. 设计目标与约束（质量基线）

### 4.1 等级面试定位
程序员客栈采用 T1–T5 技术等级认证 + 1V1 视频面试，面试官为 6 年+大厂、年薪 50 万+ 专家；「项目经验」明确来自绑定 Git 仓库展示的代表性作品。以背景应瞄准 **T3 专业级**（解决复杂问题、技术全面、具备一定架构设计思维）。仓库是核心考核证据，面试官会读代码、看结构、挑谎。

### 4.2 质量基线：MUST / NOT
**MUST**
1. 生产级参考实现（模块化、单一职责、配置驱动、有错误处理与降级），非 notebook 片段。
2. 端到端可运行：一条命令入库、一条命令问答（含轻量后端，零模型下载也能演示）。
3. 真实指标：在真实（小规模）数据上跑出真实数字——哪怕 8 份年报、50 条标注。
4. 诚实：明确标注"哪些是真实能力、哪些是示意/stub"。编造数字比没有指标更致命。
5. 可复现评测：RAGAS 或近似指标 + 对照实验脚本，别人能重跑。

**NOT**：已上线 SaaS、真实用户量、超高复杂度工程。面试官看的是工程判断力信号，不是生产流量。

### 4.3 两个真实性必须分开
| 维度 | 是否要真实企业数据 | 真正要求 |
|---|---|---|
| 数据真实性 | 不要求（公开+合成更优） | 来源合法、可开源、无 IP 风险 |
| 指标真实性 | — | 必须真实——你提交的评测脚本，在你提交的（公开/合成）数据上真实跑出，且可复现 |

**致命陷阱**：数据合成便顺手编漂亮指标（"双重伪造"），资深面试官会要求复现，一跑即穿帮。合法组合 = 合成/公开数据 + 真实指标；自毁组合 = 合成数据 + 编造指标。

### 4.4 算力约束
A/B 可全走 API（硅基流动/百炼），本机只需 Python+Docker，零本地 GPU；C 必须本地算力（3090/4090 跑 14B，或客户昇腾环境）。详见第 8 章算力总账。

### 4.5 合规边界（贯穿全程）
- 不出现现东家（弘象科技）名称、不出现电力/民航等具域名域资产，规避 NDA/竞业风险。
- 信创（昇腾+麒麟）仅作为个人技术能力呈现，不绑定雇主。
- 数据来源必须标注；真实客户文档的脱敏是实际接单项目交付项，样例阶段通过"不用真实数据"绕开。

---

## 5. 总体架构设计

### 5.1 五层架构
```
┌──────────────────────────────────────────────────────────────┐
│  L5 生成 (LLM)          Qwen3:14B 本地 / Qwen API 在线           │
├──────────────────────────────────────────────────────────────┤
│  L4 重排 (Reranker)     bge-reranker-v2-m3                      │
├──────────────────────────────────────────────────────────────┤
│  L3 向量库 (Vector DB)  Qdrant（compose / 单机，payload 过滤）    │
├──────────────────────────────────────────────────────────────┤
│  L2 嵌入 (Embedding)    BGE-M3（dense + sparse 一体）            │
├──────────────────────────────────────────────────────────────┤
│  L1 解析 (Parsing/OCR)  docling + MinerU + PaddleOCR            │
└──────────────────────────────────────────────────────────────┘
   编排：轻量自研 + Pydantic 配置驱动   服务：FastAPI + Streamlit   部署：Docker Compose
```

### 5.2 模块划分（共享底座 `rag-kit` 雏形）
| 模块 | 职责 | 被哪些样例复用 |
|---|---|---|
| `config` | Pydantic 配置驱动，tenant 级差异 | A/B/C/D |
| `parsers` | 多格式解析、OCR、噪声清洗、表格还原 | A（核心）/ B / C |
| `chunking` | 父子 Chunk、结构感知切片、表格独立节点 | A / B / C |
| `quality` | 解析/表格质量校验与降级 | A |
| `embeddings` | BGE-M3 dense+sparse 一体，可 API/本地 | 全部 |
| `retriever` | 混合检索 + 两段式重排 + 可解释召回 + payload 过滤 | 全部 |
| `generator` | 强制溯源、无据拒答、stub 双模式 | 全部 |
| `permissions` | 分级 ACL、越权拦截、审计链 | B（核心）/ C |
| `router` | 规则→7B→72B 三级路由 | D（核心） |
| `pipeline` | 摄入/问答/审计编排 | 全部 |
| `api` | FastAPI 对外服务（ingest/ask/audit） | 全部 |
| `cli` | 命令行入口 + 录屏/兜底演示 | 全部 |
| `eval` | RAGAS 式离线评测 + 对照实验 | 全部 |

### 5.3 底座复用策略
A 是底座，必须先做；B 复用 A 的解析/切片/检索/生成，仅加权限/审计层；C 复用 A/B 全部上层，仅换部署形态；D 复用 A/B 加路由层。**目标：新场景边际交付压到 20 小时内。**

---

## 6. 共享技术底座（rag-kit）说明

已封存一套脚手架（`rag-freelance-kit/rag-kit/`），覆盖解析→切片→质检→嵌入→检索→生成→权限→调度→编排→API→CLI→评测全链路，并内置 **fake 向量 + stub 生成器**，保证零下载也能端到端跑通（演示零失败的基础）。实现阶段以此为基础，按第 7 章各样例配置填充，而非从零重写。

---

## 7. 三个核心样例设计

### 7.1 样例 A · 知擎·解析 DocParse
**定位/买家**：制造业信息化部（设备手册）、法务（合同/判例）、金融研究（年报/招股书）。证明"通用方案解析错的复杂文档，我能做对"。

**数据**：巨潮资讯网（cninfo）公开年报/招股书，5–10 份即可出真实指标（路线 1）。选它是因为公开数据里版式复杂度最接近企业真实文档。

**功能（6 模块）**
1. 文档接入：PDF（文字版+扫描版）、Word、Markdown；批量入库，单文件失败降级不中断整批。
2. 解析质量：跨页表格还原为「属性-值对」；无文字层自动 OCR；页眉页脚/目录页码/水印噪声清洗；标题层级保留。
3. 检索：稠密向量+BM25 混合；两段式重排；Query 改写；可解释召回（来源+命中理由）。
4. 问答：强制溯源（来源+页码）；无据拒答。
5. 评测：解析成功率、表格还原准确率、Recall@k、Faithfulness、拒答准确率；对照实验表（纯向量→+BM25→+重排→+改写 召回率递进）。
6. 演示物料：README 指标表、3 分钟录屏、踩坑文章。

**验收标准（完整版见 `样例A_功能清单与验收标准.md`）**
| 编号 | 维度 | 指标 | 目标 |
|---|---|---|---|
| AC-1 | 解析 | 解析成功率 | ≥ 95% |
| AC-2 | 表格 | 表格还原准确率 | ≥ 85% |
| AC-3 | 检索 | Recall@10 | ≥ 85% |
| AC-4 | 生成 | Faithfulness | ≥ 0.85 |
| AC-5 | 生成 | Answer Relevancy | ≥ 0.80 |
| AC-6 | 拒答 | 拒答准确率 | ≥ 90% |
| AC-7 | 对照 | 纯向量召回递进 | 58% → 82% |

> AC-7 是 A 最有说服力的一栏：直接证明你比甲方更早知道坑在哪、以及如何解决。

**范围边界**：含模块 1–6 + 上述 7 条；不含权限/审计（B）、离线私有化/信创（C）、分层调度（D）。

### 7.2 样例 B · 知擎·合规 Guard
**定位/买家**：金融、医疗、政企合规岗。证明"95% 竞争者做不出的分级权限+审计，我能做出来"。

**数据**：路线 2（国家法律法规数据库 flk.npc.gov.cn 真实公开）+ 路线 3（自写带密级标签的"内部制度手册"样张，零版权风险）。**真实法规无密级标签，合成密级标注是 B 的必要组成，非偷懒。**

**功能**
1. 分级权限模型：public/internal/restricted 三级 + 三角色（访客/员工/管理员）。
2. 越权拦截：基于密级标签的检索/问答级访问控制，在检索层 payload 硬过滤（非生成后过滤），越权拦截率 100%（测试用例覆盖）。
3. 三角色演示：同一问题不同角色看到不同密级答案。
4. 强制溯源 + 无据拒答（复用 A 基线）。
5. 操作审计：不可篡改审计日志（谁/何时/问了什么/命中哪些片段/是否被拦截），追加写+哈希链。
6. 风控熔断：Prompt 注入检测、敏感词过滤、频次限制（加分）。

**验收指标**
| 指标 | 目标 |
|---|---|
| 越权访问拦截率 | **100%**（硬指标） |
| Faithfulness | ≥ 0.85 |
| 溯源可点击率 | 100% |
| 拒答准确率 | ≥ 90% |
| 审计日志完整率 | 100% |
| Prompt 注入拦截率 | ≥ 95%（20 条样本） |

### 7.3 样例 C · 知擎·信创 Edge
**定位/买家**：政企/国企/数据不出内网机构（单价最高 8–30 万）。证明"断网可用、一键起、还能跑在信创环境"。

**数据**：复用 A/B 数据，只换部署形态，边际成本极低。

**功能**
1. 离线一体包：`docker compose` 一键起（向量库+推理+API）。
2. 断网可用：无外网依赖，断网自测通过。
3. 本地推理：Ollama/vLLM 起 Qwen3:14B（可换模型、可量化）。
4. 信创适配（独有）：昇腾 NPU + 麒麟 OS 实测部署与性能数据——简历最宽护城河，竞争者几乎为零。
5. 资源画像：不同硬件配置下的性能/显存数据（必须实测，不抄）。

**验收指标**
| 指标 | 目标 |
|---|---|
| 部署耗时 | ≤ 30 分钟（裸机到可提问） |
| 断网可用性 | 100% |
| 首 Token 延迟 | ≤ 1.5s（14B INT4，单卡 4090） |
| 量化精度损失 | ≤ 3%（对比 FP16） |

### 7.4 样例 D · 知擎·调度 Router（可选加分项）
**定位/买家**：成本敏感客户。复刻简历"规则引擎+7B+72B 三层分工"，证明能把最贵模型调用砍半。
**功能**：规则/关键词 → 7B 轻模型 → 72B 重模型三级路由，按置信度升级，并发高峰自动降级。
**验收**：L2 调用占比 ≤ 30%；综合成本下降 ≥ 50%；准确率损失 ≤ 2%；P95 ≤ 3s。建议拿到前 2–3 个单子后再做。

---

## 8. 技术栈选型（逐层 + 算力）

| 层 | 推荐 | 备选 | 许可 | 本地算力 |
|---|---|---|---|---|
| L1 解析 | docling（主）+ MinerU（中文复杂表）+ PaddleOCR（扫描件） | MarkItDown / unstructured | Apache/MIT | OCR 阶段轻量 GPU，CPU 可跑 |
| L2 嵌入 | **BGE-M3**（dense+sparse 一体，混合检索免搭 BM25） | Qwen3-Embedding-0.6B/8B | MIT / Apache 2.0 | 568M，CPU 可跑、GPU 更快 |
| L3 向量库 | **Qdrant 单机 Docker**（payload 过滤支撑 B 权限） | Milvus / Chroma / FAISS / pgvector | Apache 2.0 | 几乎不吃，CPU |
| L4 重排 | **bge-reranker-v2-m3**（默认正确项） | Qwen3-Reranker | Apache 2.0 | GPU 友好，CPU 可跑 |
| L5 生成 | **Qwen3**（在线 API 演示 / 离线 Ollama-vLLM:14B） | DeepSeek / Llama / GLM | Apache 2.0 | 14B≈12–16G 显存（1×4090） |
| 编排 | **轻量自研 + Pydantic 配置驱动** | LlamaIndex 兜底 | — | 无 |
| 服务 | FastAPI（必做，工程能力证据）+ Streamlit（薄 UI）+ Docker Compose | — | — | 无 |

**算力总账**
| 阶段 | 本地算力 | 说明 |
|---|---|---|
| 开发期 / A、B 演示 | 可零 | 嵌入/重排/LLM 走 API，向量库用内存或云端 |
| 跑一次真实指标 | 不需要 own GPU | 同套 API 跑一遍记录数字 |
| 样例 C 离线/信创 | 必须 | 3090/4090 跑 14B，或客户昇腾环境 |
| 最低可行配置 | 16G 内存 + 普通 CPU 笔记本 | 开发+接 API 跑通；本地跑 14B 再+24G 显卡 |

**商用授权**：BGE 系列（MIT/Apache）、Qwen 系列（Apache 2.0）、Qdrant（Apache 2.0）、docling（Apache 2.0）均可商用，无授权风险。

---

## 9. 数据方案

| 路线 | 内容 | 用于 | 关键说明 |
|---|---|---|---|
| 路线 1 | 巨潮网 cninfo 公开年报/招股书（AJAX 接口免登录，返回 PDF 直链） | 样例 A | 5–10 份即出真实指标；标注来源、限流、不商业再分发 |
| 路线 2 | 国家法律法规数据库 flk.npc.gov.cn 真实公开 | 样例 B 内容 | 真实法规无密级标签，需合成标注 |
| 路线 3 | 自写带密级标签的"内部制度手册"样张 | 样例 B ACL 演示/兜底 | 零版权风险、完全可控、便于确定性评测 |

**脱敏原则**：样例阶段用"不用真实数据"直接绕开脱敏负担；但可把脱敏做成管道内的**功能模块**（自动遮蔽身份证/手机号/金额）作为能力演示加分项。Git 不塞大 PDF（.gitignore + 外部下载脚本）。

---

## 10. 演示与界面方案

- **面试不强制现场演示**：核心评价是 Git 仓库（README+结构+测试+评测+指标）。能"点开看一眼"远胜纯口述，但不把"现场 live 跑全栈"当主力（网络/API key/延迟任一出岔即尬场）。
- **已定策略：录屏 + 1 个 Streamlit 旗舰**（不做三个 Web）。旗舰 UI 优先挂 A（知擎·解析）；B/C 靠仓库+README+录屏覆盖。
- **零失败设计**：生成器双模式——`stub` 后端断网/无 key 也能跑（展示检索/溯源/权限拒绝/审计）；真实 LLM 走离线一次跑、指标写入 README。**UI 必须调真实 pipeline，禁写死假前端。**
- **UI 内容清单**：上传文档→看 chunks（解析质量）；提问→答案+引用（溯源）；(B)切换角色→不同密级答案+审计日志。

---

## 11. 开发路线与里程碑

| 阶段 | 内容 | 工时 | 建议节奏 |
|---|---|---|---|
| 阶段 1 | 底座 + 样例 A | 25h | 第 1–2 周末 |
| 阶段 2 | 样例 B | 20h | 第 3–4 周末 |
| 阶段 3 | 样例 C | 15h | 第 5 周末 |
| 阶段 4 | 加分项 D | 10h | 有余力再做 |
| 阶段 5 | 物料+主页+试投 | 8h | 与阶段 3 并行 |

> **求职期提醒**：简历标注"在职·月内到岗"。样例的复利价值在于"既是接单作品集、也是面试作品集"，一套投入两处收益；但若时间紧张，**优先保证求职**，做到 A+B 即形成有效背书，C/D 拿到 offer 后补。

---

## 12. 面试与接单应用

**面试叙事（约 180 字，可直接背）**
> "我做了一个企业级 RAG 知识库框架『知擎』，用三个递进样例证明三件事：第一，我能把最难解析的企业文档——扫描件、跨页表——做对，召回率从纯向量的 58% 提到 82%；第二，我能满足强监管行业的分级权限和不可篡改审计，越权拦截率 100%；第三，我能做到断网可用、一键部署、还能跑在昇腾+麒麟信创环境。三个样例共用一套底座，新场景边际交付能压到 20 小时内。"

**接单策略（80/20）**：20% 精力在平台接 2–3 个 RAG 订单积累成交记录与评价（低于 1500 元/人天不接）；80% 精力用"架构评审/选型咨询/两周 POC"作钩子转私有化部署整包（咨询时薪是写代码接单的 6–10 倍）。坚决不碰 C++ 桌面端、低代码 Agent 拖拽、纯爬虫清洗。

---

## 13. 合规边界（贯穿全程）

1. 不出现现东家名称、不出现电力/民航等具域名域资产——规避 NDA/竞业。
2. 信创（昇腾+麒麟）仅作个人技术能力呈现，不绑定雇主。
3. 数据来源必须标注；真实客户文档脱敏属实际接单交付项，样例阶段不用真实数据绕开。
4. 平台抽成 8.6%–10%，报价预留。

---

## 14. 附录：文档清单与衔接

| 文件 | 内容 | 状态 |
|---|---|---|
| `知擎RAG框架_整体软件设计方案.md` | 本文，总纲 | 已收口 |
| `样例命名与背景设定.md` | 名称/功能/背景叙事 | 已锁定 |
| `样例达标标准与数据方案.md` | T3 基线 + 数据三路线 + 真实性原则 | 已锁定 |
| `样例A_功能清单与验收标准.md` | A 完整功能+验收（AC1-7） | 已锁定 |
| `样例技术栈与算力说明.md` | 五层选型 + 算力总账 | 已锁定 |
| `面试演示与界面方案.md` | 录屏+1 Streamlit+stub 零失败 | 已锁定 |
| `样例项目设计_3+1.md` | 早期 3+1 设计（已被命名/技术栈文档取代，保留参考） | 参考 |
| `RAG接单作战手册.md` / `交付物料模板.md` | 接单话术与交付物料 | 已具 |
| `rag-kit/` | 脚手架雏形（解析→评测全链路 + stub/fake 后端） | 封存待实现 |

**下一步**：B/C 功能文档按本文第 7.2/7.3 节细化收口 → 三份功能文档齐备 → 进入实现（以 `rag-kit` 底座为基础）。
